#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${repo_root}/backend/.venv/bin/python" "${repo_root}/backend/scripts/report_gemini_study.py" "$@"
