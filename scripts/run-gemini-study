#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
backend_root="${repo_root}/backend"

exec "${backend_root}/.venv/bin/python" "${backend_root}/scripts/run_gemini_study.py" "$@"
