# Only variables intentionally compiled into the browser belong here.
VITE_VOLUNDR_GENERATION_MODE=advanced
VITE_VOLUNDR_CHAT_FIRST=true

# Playwright/live harness overrides are supplied by scripts, not production
# browser configuration:
# VOLUNDR_E2E_PORT=8000
# VOLUNDR_E2E_WEB_PORT=5173
