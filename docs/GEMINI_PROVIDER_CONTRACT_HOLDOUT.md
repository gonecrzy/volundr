# Gemini Provider Contract Holdout

The holdout is excluded from settings and prompt selection: three
requirements packets, three Plan packets, three geometry packets, and one
repair packet, each repeated twice. It ran 20 logical operations with the
selected S0/H0/T0 combination.

Results: 20/20 logical operations completed, 23 provider attempts, exact
model identity on successful calls, and 17/20 intrinsic quality passes. The
failures were a critical clarification invention, a conflicting Plan identity
result, and an incomplete repair response. One transport operation also
reached its two-attempt limit and remained excluded from content scoring.

Because the universal foundation floor requires every holdout response to
clear, the provider decision is `provider_contract_not_yet_stable` and the
adapter decision is `provider_contract_requires_revision`. No end-to-end
workflow or production deployment follows from this run.
