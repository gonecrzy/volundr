# Gemini model comparison

Status: pending benchmark execution.

Model comparison will report separate response-structure, requirements,
planning, execution, and outcome scores. It will preserve field-level
classifications and normalized failure signatures rather than collapsing
provider variability into a single success rate.
