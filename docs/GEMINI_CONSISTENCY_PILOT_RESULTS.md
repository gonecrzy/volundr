# Gemini consistency pilot results

Status: pending the API-only pilot execution.

This document is reserved for the two-run, ten-case pilot result for every
selected model. It must record the exact experiment ID, model discovery result,
identity snapshot, case outcomes, clarification and retry counts, integrity
findings, separate consistency scores, and the decision to proceed or stop.

Raw prompts, provider responses, source, worker output, screenshot metadata,
and network evidence remain local under the durable data root and outside Git.
