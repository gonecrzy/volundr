# Mixed-CAD convergence correction plan

Planning only. This document is the expected repair plan; it does not
authorize corrections in the current run and no correction was applied during
or between the controlled batches.

## One deferred next priority

The sole deferred next priority is **integrity/misleading-state reconciliation
between generation-attempt lifecycle and frozen batch outcomes**.

Expected repair scope:

- derive `Not started`, blocked, and incomplete outcomes from the complete
  project → workflow → generation-attempt chain;
- reconcile non-terminal attempts at report/freeze time without inventing a
  provider or worker result;
- make report, drawer, comparison, and `codex-review.md` use the same terminal
  outcome vocabulary;
- preserve the original raw, normalized, repaired, and final response
  evidence while recording the reconciliation as an integrity finding;
- add deterministic coverage for an attempt that remains started, report
  regeneration, frozen membership, and no-provider/no-worker report
  generation; and
- run a separate verification pair only after the repair is reviewed. It must
  not be mixed with CAD-family fixes.

## Classification of remaining work

- Repeated cross-product: provider responses still cross the semantic,
  provenance, source-contract, and design-artifact boundaries after envelope
  normalization.
- Repeated same-family: holder topology/drainage, organizer source features,
  monitor Plan provenance, screw-lid grip/thread, and wall-carrier source
  identity remain family-level observations.
- Provider variability: the same prompt positions reached different stages and
  failure signatures between the controlled runs.
- Isolated anomalies: unsupported pattern helper, invalid escape, missing
  result-symbol assignment, and restricted lid-grip-ribs function are retained
  as regression candidates.
- Integrity/misleading-state: the Batch 2 screw-lid `Not started` result
  conflicts with preserved attempts and is the only deferred next priority.

No item above authorizes changes to product-family CAD behavior. The
monitor-wall-mount project remains a geometry/workflow evaluation and never a
load-bearing safety claim; physical engineering and test review remain
mandatory.
