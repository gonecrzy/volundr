# Gemini response variability

Status: pending benchmark execution.

The comparison will classify each paired field as identical,
semantically equivalent, acceptably variable, materially inconsistent,
one-sided failure, both failed with the same signature, or both failed with
different signatures. Numeric tolerances and ID-keyed list ordering are
explicit; no silent prose-only equivalence is used for dimensional or output
structure claims.
