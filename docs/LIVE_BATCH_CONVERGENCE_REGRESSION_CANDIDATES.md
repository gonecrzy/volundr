# Mixed-CAD convergence regression candidates

Planning-only list from the two frozen batches. These candidates are not
implemented by this run.

1. Report lifecycle reconciliation: a project with provider/source attempts
   must not be rendered as `Not started` merely because the final attempt is
   non-terminal.
2. Source-generation contract candidate: preserve and classify unsupported
   helper calls such as `rectangular_pattern_points` without implying worker
   execution.
3. Geometry-body extraction candidate: preserve the invalid-escape and
   missing-result-symbol diagnostics as distinct provider/source contract
   failures.
4. Repair convergence candidate: retain unchanged and regressive repair
   outcomes while making the final report's project outcome agree with the
   attempt chain.
5. Product-family follow-up candidates: portable-holder topology/drainage,
   organizer feature/source extraction, monitor Plan provenance, and
   screw-lid grip/thread generation.

Candidate 1 is the only deferred next priority. The others are recorded for a
separate planning decision and are not authorized for implementation here.
