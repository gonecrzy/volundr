# Gemini ablation evaluator correction

The original evaluator was preserved under
`reports/historical/pre-buildability-reanalysis/` before correction.

The corrected evaluator now uses authoritative packet expectations authored
from the frozen prompts, fact sheets, accepted requirements, protected
identities, and stage contracts. It no longer derives expected meaning from
provider responses.

Corrections include:

- Packet 2 capacity, dimensions, loading direction, handle, drainage, strap
  slots, open walls, and single-holder semantics.
- Packet 3 authoritative slot IDs `1`–`4`, including equivalent radius and
  derived-wall constructions.
- Empty nested-object and empty-ready-plan rejection.
- Cross-field identity and source-contract validation.
- Absolute quality-floor outcomes separate from buildability and consistency.
- Separate semantic and byte-level repeatability.
- Buildability dimensions with explicit weights.
- Baseline-ceiling-safe stable-foundation qualification.

All 30 records were rescored offline with zero provider calls. Original raw
responses and immutable captures were not modified.
